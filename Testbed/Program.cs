using RenderSharp;
using RenderSharp.EntityComponentSystem;

using System.Text.Json;
using System.Text.Json.Serialization;

Window window = new Window();

//HelloTriangle triangle = new HelloTriangle();

//Scene scene = new Scene();

//var options = new JsonSerializerOptions { WriteIndented = true, ReferenceHandler = ReferenceHandler.Preserve };
//string jsonString = JsonSerializer.Serialize(scene, options);

//System.IO.File.WriteAllText("SerializedScene.json", jsonString);